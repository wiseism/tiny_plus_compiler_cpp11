# 语法分析思路

## 幻灯片 1
           编译原理实验（语法分析）
李林杰

## 幻灯片 2
   实验一&实验二
实验1以字符为分析对象，我们将这些字符根据词法规则划分成一个一个词（token）。
实验2以token为分析对象，我们将这些token根据语法规则构造一语法树。
语法树具有特定的规则，可通过它还原成一条一条的语句，还可以生成中间代码。

## 幻灯片 3
  实验内容
实验内容：
      TINY+语言的语法分析、语义分析、中间代码生成
实验步骤：
1、对实验1中token细化。
2、定义符号表，存储标识符。
3、定义语法树，采用递归子程序法构造语法树。
4、语法分析，根据“TINY+ new.doc”中语法规则。
5、语义分析，在语法分析的函数中增加些判断。
6、中间代码生成，遍历语法树对应生成。

## 幻灯片 4
  对实验一中的token进行细化
请大家按文件“TINY+ new.doc”里的说明对实验一的（Kind，Value）的Kind值进行细化。
实验1：  （KEY，if）
实验2：  （TK_IF，if）

## 幻灯片 5
  对实验一中的token进行细化
下面是所有要细化的token：
(TK_TRUE, true), (TK_FALSE, false), (TK_OR, or)
(TK_AND, and), (TK_NOT, not), (TK_INT, int),
(TK_BOOL, bool), (TK_STRING, ’…’), (TK_WHILE, while),
(TK_DO, do), (TK_IF, if), (TK_THEN, then),
(TK_ELSE, else), (TK_END, end), (TK_REPEAT, repeat),
(TK_UNTIL, until), (TK_READ, read), (TK_WRITE, write)
(TK_GTR, >),  (TK_LEQ, <=),  (TK_GEQ, >=)
(TK_COMMA, ,),  (TK_SEMICOLON, ;),  (TK_ASSIGN, :=),
(TK_ADD, +),  (TK_SUB, -),  (TK_MUL, *),
(TK_DIV, /),  (TK_LP, (),  (TK_RP, )),
(TK_LSS, <),  (TK_EQU, =)

## 幻灯片 6
  对实验一中的token进行细化
可以使用C++里的vector容器vector<Token*> tokens 将所有token存储起来。
typedef struct
{
	int     kind;		// 符号的Kind值，如TK_IF等
	int     ival;		// 若符号是整型常量，则ival记录该				             // 常量的值
	char   *sval;	        	// 若符号是标识符或字符串常量，则				        	// sval记录标识符的名字或字符串
}Token;

## 幻灯片 7
  定义符号表、存储标志符
首先定义一个结构体存放符号的各种属性：
typedef struct
{
	Token *tk;	                       // 符号(词法分析器的返回值)
	ObjType objtype;	 // 符号对象类型：函数/变量/常量
	ValType   valtype;     	 // 值类型: int/bool/string
     int	 addr;	 // 地址。
     int	 size;	 // 字节数
     int	 level;	 // 层次
}Sym;
其中objtype可以取以下值：
enum ObjType
{
	OT_FUN,	           	   // 函数
	OT_VAR,	            	   // 变量
	OT_CONST,	   // 常量
};

## 幻灯片 8
  定义符号表、存储标志符
其中valtype可以取以下值：
enum ValType
{
	VT_INT,		// 整型数类型
	VT_BOOL,		// 布尔类型
	VT_STRING,	         	// 字符串类型
};
最终符号表将是一个用来存储结构体Sym的容器，可以使用C++里的vector容器。
如最终的符号表定义如下：vector< Sym* > symtable;
不过建议写一个SymTable的类来处理符号表，因为符号表需要有插入、查找、删除操作。

## 幻灯片 9
  定义符号表、存储标志符
需要实现的符号表操作函数：
插入函数：Sym* insertSym(char* name);
    实现插入新符号到符号表的操作。其中name是新符号的名字，插入成功后返回该符号的Sym指针，失败则返回null。
查找函数：Sym* findSym(char *name);
    实现查找功能。在符号表中查找名字为name的符号并返回该符号的Sym指针，失败则返回null。
删除函数：void delSym(char *name);
    实现删除功能。在符号表中删除名字为name的符号。

## 幻灯片 10
  定义语法书
定义一个结构体作为语法树的节点：
typedef struct _TreeNode
{
	NodeType    nodetype;     // 节点类型
	ValType     valtype;	    // 节点值类型
	TreeNode    *child[3];	    // 子节点的指针
	Token        *tk;		   // 当节点是FACTOR类型时该成员才有效
} TreeNode;

## 幻灯片 11
  语法树节点类型
enum NodeType
{
	PROGRAM,	 // 程序（开始符号）节点
	STMT_SEQUENCE, // 语句列表节点
	IF_STMT,	 // 条件语句节点
	REPEAT_STMT,	 // repeat语句节点
	ASSIGN_STMT,	 // 赋值语句节点
	READ_STMT,	 // read语句节点
	WRITE_STMT,	 // write语句节点
	WHILE_STMT,	 // while语句节点
    GTR_EXP,	                   // 大于表达式节点
	GEQ_EXP,	// 大于等于表达式节点
	LSS_EXP,		// 小于表达式节点
	LEQ_EXP,	// 小于等于表达式节点
	LOG_OR_EXP,	// 逻辑或表达式节点
	LOG_AND_EXP,	// 逻辑与表达式节点
	LOG_NOT_EXP,	// 逻辑非表达式节点
	ADD_EXP,	// 加法表达式节点
	SUB_EXP,	// 减法表达式节点
	MUL_EXP,	// 乘法表达式节点
	DIV_EXP,		// 除法表达式节点
	FACTOR		// 原子节点
};
其中nodetype可以取以下值（从TINY+语法规则中提取得出）：

## 幻灯片 12
  语法分析
由于我们采用的是递归下降的分析方法，这种方法一般是把文法的每个产生式对应实现一个分析函数（有时候多个产生式合起来写成一个分析函数）。
下面以伪代码的形式给出以下TINY+文法的分析函数：
program -> declarations stmt-sequence
declarations -> decl ; declarations |ε
decl -> type-specifier varlist
type-specifier -> int | bool | string
varlist -> identifier [ , varlist ]
stmt-sequence -> statement [ ; stmt-sequence]
…
if-stmt -> if  logical-or-exp then stmt-sequence [else stmt-sequence] end
factor  -> number | string | identifier | true | false| ( logical-or-exp )

## 幻灯片 13
  语法分析
开始语法分析前，先准备点小工具：
定义一个全局变量Token token；用于存放当前正在分析的符号；
实现一个void GetToken();函数用于从词法分析器读入一个Token并存放在全局变量token里。
      void GetToken(){
	     token = NextToken();
      }

## 幻灯片 14
  语法分析
实现一个bool match(int kind);函数用于匹配当前正在分析的符号（存放在token变量中）的kind值是否属于集合kind_set；若是则返回true并跳过该符号，否则返回false。
bool match(kind_set){
	if(token.kind in kind_set){
			      GetToken();
		      return true;
	}else{
		      error(“Token.kind类型不匹配.”);
		      return false;
	}
}

## 幻灯片 15
  program产生式的分析函数
program是开始符，它对应的分析函数的返回值就是整个程序的语法树了。
program的伪代码：
TreeNode* program(){
	declarations();			// 调用declarations函数分析变量的声明
	return stmt_sequence();	// 调用stmt_sequence()分析语句列表
					// stmt_sequence()的返回值就是整个
					// 程序的语法树（不包括声明部分）
}

## 幻灯片 16
生式declarations、 decl、 type-specifier、varlist合起来写成一个分析函数
声明的分析函数：
void declarations(){
	while(token.kind in {TK_INT,TK_BOOL,TK_STRING}){
		     GetToken();	                        	// 跳过类型说明
         do{                                      		//符“int,bool,string”
			          match(TK_ID);
		     }while(token.kind == TK_COMMA); //逗号
		     match(TK_SEMICOLON);	       	 // 匹配分号
	}
}

## 幻灯片 17
stmt-sequence产生式的分析函数
TreeNode* stmt_sequence(){
	 TreeNode *t1=null,*t2=null;
	// 语句开始符
	stmt_first={TK_IF,TK_WHILE,TK_REPEAT,TK_ID,TK_READ,TK_WRITE};
	Token first = token;
	while(match(stmt_first)==true){
		      // 注意：语句的开始符“if、while”等已由上面的match语句匹配
		      // 因此下面各语句的分析函数（if_stmt()等）不需要匹配开始符。
		      switch(first.kind){
		            case TK_IF:        t2=if_stmt();	break;
		            case TK_WHILE: t2=while_stmt(); break;
		            …
		      }
		      // 见下页

## 幻灯片 18
stmt-sequence产生式的分析函数
		      if(t1==null)
			           t1 = t2;
		      else{
			           	    // 构建STMT_SEQUENCE节点,
				   //t1,t2分别为左右孩子
			           t1 = new TreeNode(STMT_SEQUENCE,t1,t2);
		      }
	}
	return t1;
}

## 幻灯片 19
if-stmt产生式的分析函数
TreeNode* if_stmt(){
	// 不需要调用 GetToken();匹配“if”符号，原因参见stmt_sequence()
	TreeNode *cond_exp=null,*then_stmt=null,*else_stmt=null;
	cond_exp = log_or_exp();	 		// 分析条件表达式部分
	then_stmt = stmt_sequence();		// 分析then部分
	if(token.kind == TK_ELSE){
		      else_stmt = stmt_sequence();	// 分析else部分
	}
	//构建IF_STMT节点，有3个孩子，分别是条件表达式、then语句和
	// else语句；若没有else语句，则第三个孩子为null
	return new TreeNode(IF_STMT, cond_exp, then_stmt, 	else_stmt);
}

## 幻灯片 20
factor产生式的分析函数
TreeNode* factor(){
	factor_first={TK_INT,TK_STRING,TK_ID,TK_TRUE,TK_FALSE, TK_LP};
	if(token.kind not factor_first){
		error(“非法符号.”);
		return null;
	}
	TreeNode *t1=null;
	if(token.kind == TK_LP){
		       match(TK_LP);
		       t1 = new TreeNode(FACTOR,log_or_exp());
		       match(TK_RP);
	}
         else {		         // factor  -> number | string | identifier | true | false
		       t1 = new, TreeNode(FACTOR ,token);	// token 做为FACTOR节点的孩子
		       match(factor_first);
	}
	return t1;
}

## 幻灯片 21
语义分析
完成语法分析后，就可以根据属性文法，把属性规则写到各个产生式的分析函数里。
接下来将修改前面写的分析函数使其实现语义分析。
以下分析函数已不需要修改：
program()、 stmt_sequence()

## 幻灯片 22
声明declarations()的语义分析
void declarations(){
	while(token.kind in {TK_INT,TK_BOOL,TK_STRING}){
		    Token type = token;	                        // 保存类型的符号
		    GetToken();
		do{
			    Token id = token;		     // 保存标识符的符号
			    if(match(TK_ID)==true){
				        Sym *sym = insertSym(id.sval); // 插入符号表
				        // 填写符号的各种属性
				        sym->tk = id;
				        sym->objtype = OT_VAR;         //变量
				        sym->valtype = {type.kind对应的VT_XX值}
			    }
		}while(token.kind == TK_COMMA);
		match(TK_SEMICOLON);
	}
}

## 幻灯片 23
条件语句if_stmt()的语义分析
TreeNode* if_stmt(){
	// 不需要调用 GetToken();匹配“if”符号，原因参见stmt_sequence()
	TreeNode *cond_exp=null,*then_stmt=null,*else_stmt=null;
	cond_exp = log_or_exp();            // 分析条件表达式部分
                                                       //运算的规则：先乘除----》加减----》关系----》逻辑；
	then_stmt = stmt_sequence();	 // 分析then部分
	if(token.kind == TK_ELSE){
		else_stmt = stmt_sequence();       // 分析else部分
	}
	// 语义检查
	if(cond_exp->valtype != VT_BOOL)
		      error(“表达式的值必须是bool类型.”);
	//构建IF_STMT节点，有3个孩子，分别是条件表达式、then语句和
	// else语句；若没有else语句，则第三个孩子为null
	return new TreeNode(IF_STMT, cond_exp, then_stmt, else_stmt);
}

## 幻灯片 24
factor()产生式的分析函数
TreeNode* factor(){
	factor_first={TK_INT,TK_STRING,TK_ID,TK_TRUE,TK_FALSE, TK_LP};
	if(token.kind not in factor_first){
		       error(“Illegal Token.”);
		       return null;
	}
	TreeNode *t1=null;
if(token.kind == TK_LP){
		       match(TK_LP);
		       TreeNode *exp = log_or_exp();
		       t1 = new TreeNode(FACTOR, exp );
		       t1->valtype = exp->valtype;
		       match(TK_RP);
	}else  // factor  -> number | string | identifier | true | false
		       t1 = new TreeNode(FACTOR, token);	                   // token 做为FACTOR节点的孩子
		       t1->valtype = {token.kind对应的VT_XX值}
		       match(factor_first);
	}
	return t1;
}

## 幻灯片 25
各产生式的返回值
if_stmt()              new TreeNode(IF_STMT, cond_exp, then_stmt, else_stmt) ;
repeat_stmt()      new TreeNode( REPEAT_STMT , rep_stmt , cond_exp ) ;
assign_stmt()      new TreeNode( ASSIGN_STMT , cond_exp ) ;
read_stmt()         new TreeNode( READ_STMT ) ;
write_stmt()         new TreeNode( WRITE_STMT , cond_exp ) ;
while_stmt()         new TreeNode( WHILE_STMT , cond_exp , rep_stmt ) ;
exp()//表达式       new TreeNode( LOG_AND/OR/NOT_EXP , t1 , t2 ) ;
comparison_exp() new TreeNode( GTR/GEQ/LSS/LEQ/EQU_EXP , t1 , t2 ) ;
arithmetic_exp()    new TreeNode( ADD/SUB_EXP , t1 , t2 ) ;
simple_exp()          new TreeNode( MUL/DIV_EXP , t1 , t2 ) ;
factor()                  new TreeNode( FACTOR ) ;

## 幻灯片 26
中间代码生成
中间代码生成程序将任何一段TINY+ 程序翻译成三地址中间代码。
中间代码的生成，由于处理顺序的不同，代码的标号可能和示例的结果不完全一致，只要符合逻辑就都是正确的。

## 幻灯片 27
中间代码生成
代码生成的主要思想是用后缀遍历法遍历整棵语法树，生成各节点对应的代码序列即可。
如节点类型是ADD_EXP，
表明这是一个加法表达式，
加法表达式形式是：左部+右部，
可表示成右边的语法树，子树left是“左部”的语法树，right是“右部”的语法树。
ADD_EXP
left
right

## 幻灯片 28
中间代码生成
由于我们采用后缀遍历法，当处理节点ADD_EXP时，其两个子树left、right已经处理完了，也就是说子树的代码已经生成好了。从代码运行的角度来看就是“左部”和“右部”的结果已经在栈顶了。
所以对于ADD_EXP节点，只需生成一个指令“ADD”就可以了。

## 幻灯片 29
各节点的代码生成
下面两个类型的节点不需要生成代码，只需要遍历它的子节点即可。
PROGRAM
STMT_SEQUENCE
其他语句的代码生成可以参考：
TINY源代码PARSE.C部分。

## 幻灯片 30
收集运行时数据
运行时需要的数据有：
代码（上面的代码生成得到的代码）
静态数据（字符串常量）
全部变量的个数（运行时用于分配全局数据区）
收集方法：
代码生成已得到最终的代码，无限再收集；
对于静态数据：由于值存放字符串常量，因此我们可以用一个字符串数组来存放这些字符串常量，源代码里出现的第一个字符串常量存放到该数组的第0个元素里，第二个存放到第1个元素里，以此类推。当然，在代码生成的时候，字符串常量的地址我们就可以使用这个数组的下标来表示了；
对于全局变量的个数：符号表里全局变量的个数既是。

## 幻灯片 31
七、实现虚拟机
栈式计算机介绍
请参考“关于PL0编译器.doc”的“运行时数据栈的工作原理”。注意：文档里提到的“虚拟堆栈机”既是“栈式计算机”；“运行时数据栈”即是前面提到的“运行时栈”。

## 幻灯片 32
7.1、虚拟机定义
class VM
{
public:
	void Init(PCode *code,char** staticData,int globalDataCount);
	void Run();
private:
	int Pop() { return stack[SP--]; }
	void Push(int val) { stack[++SP] = val; }
	static const int STACK_MAX_SIZE = 2048;		// 栈的大小
	PCode	*code;		                       	// 代码区指针
	int		IP;			// 指令寄存器
	int		*stack; 			// 运行时栈
	int		SP;			// 栈顶寄存器
	int		*globalData;		// 全局数据区
	char		**staticData;		// 静态数据区
};

## 幻灯片 33
7.2、运行时环境初始化（Init）
void VM::Init(PCode *code,char** staticData,int globalDataCount)
{
	把 code 拷贝到 this->code
	IP = 0;
	把 staticData 拷贝到 this->staticData
	 globalData = new int[globalDataCount];
	stack = new int[STACK_MAX_SIZE];
	SP = 0;
}

## 幻灯片 34
7.3 Run函数实现1
void VM::Run()
{
	PCode c;
	int opnd1,opnd2;
	do
	{
		c = code[IP++];	// 取一条指令
		switch( c.type)
		{
		case CT_ADD:
			opnd1 = Pop();
			opnd2 = Pop();
			Push(opnd1+opnd2);
			break;
		case CT_SUB:
			opnd1 = Pop();
			opnd2 = Pop();
			Push(opnd1-opnd2);
			break;
		case CT_SUB:
			opnd1 = Pop();
			opnd2 = Pop();
			Push(opnd1-opnd2);
			break;
		case CT_MUL:
			opnd1 = Pop();
			opnd2 = Pop();
			Push(opnd1*opnd2);
			break;
		case CT_DIV:
			opnd1 = Pop();
			opnd2 = Pop();
			Push(opnd1/opnd2);
			break;

## 幻灯片 35
7.3 Run函数实现2
		case CT_HALT:
			break;
		case CT_JMP:
			IP += c.opnd-1;		// 前面有个 IP++, 故这里需减1
			break;
		case CT_JZ:
			opnd1 = Pop();
			if(opnd1 == 0)
				IP += c.opnd-1;	// 前面有个 IP++, 故这里需减1
			break;
		case CT_JNZ:
			opnd1 = Pop();
			if(opnd1 != 0)
				IP += c.opnd-1;	// 前面有个 IP++, 故这里需减1
			break;
		case CT_IN:
			cin >> opnd1;
			Push(opnd1);
			break;
		case CT_OUT:
			opnd1 = Pop();
			cout << opnd1;
			break;

## 幻灯片 36
7.3 Run函数实现3
		case CT_OUTSTR:
			cout << staticData[c.opnd];
			break;
		case CT_LDC:
			Push(c.opnd);
			break;
		case CT_LDV:
			Push(globalData[c.opnd]);
			break;
		case CT_ST:
			globalData[c.opnd] = Pop();
			break;
		case default:
			printf("Bad CodeType\n");
			return;
		}
	}while(c.type != CT_HALT)
}

## 幻灯片 37
开始实验

